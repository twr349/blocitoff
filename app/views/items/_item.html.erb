<div class="item">
  <%= item.name %>
  <h4><%= distance_of_time_in_words(Time.now, item.created_at + 7.days) %>  <span class="glyphicon glyphicon-time"></span></h4>
  <%= link_to "", [item.user, item], method: :delete, class: 'glyphicon glyphicon-ok', :remote => true %>
</div>